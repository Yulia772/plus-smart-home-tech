server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/commerce?currentSchema=warehouse
    username: commerce
    password: commerce
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  sql:
    init:
      mode: always